#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main()
{
    string s;
    cin >> s;
    set<char> st;
    for (char c : s)
    {
        st.insert(c);
    }
    int mini = 100;
}